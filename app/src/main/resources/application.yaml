spring:
  application:
    name: career-claw
management:
  endpoint:
    health:
      probes:
        enabled: true
      show-details: never
  endpoints:
    web:
      exposure:
        include: health
server:
  forward-headers-strategy: framework
