name: Daily Korea Tech Brief

on:
  schedule:
    - cron: "47 8 * * 1-5"
      timezone: "Asia/Seoul"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: career-feed-kr-tech-daily-${{ github.ref }}
  cancel-in-progress: false

jobs:
  kr-tech-daily:
    name: Generate and send Korea tech daily brief
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          persist-credentials: true

      - name: Log workflow trigger context
        shell: bash
        env:
          EVENT_NAME: ${{ github.event_name }}
          EVENT_SCHEDULE: ${{ github.event.schedule }}
        run: |
          set -euo pipefail
          echo "event_name=${EVENT_NAME}"
          echo "event_schedule=${EVENT_SCHEDULE:-}"
          echo "kst_now=$(TZ=Asia/Seoul date '+%Y-%m-%d %H:%M:%S %Z')"

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.x"

      - name: Check required secrets
        shell: bash
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          DISCORD_WEBHOOK_KR_TECH_DAILY: ${{ secrets.DISCORD_WEBHOOK_KR_TECH_DAILY }}
        run: |
          set -euo pipefail

          missing=()
          if [ -z "${OPENAI_API_KEY}" ]; then
            missing+=("OPENAI_API_KEY")
          fi
          if [ -z "${DISCORD_WEBHOOK_KR_TECH_DAILY}" ]; then
            missing+=("DISCORD_WEBHOOK_KR_TECH_DAILY")
          fi

          if [ "${#missing[@]}" -gt 0 ]; then
            printf 'Missing required secret(s): %s\n' "${missing[*]}" >&2
            exit 1
          fi

      - name: Prepare report directories
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p reports/candidates reports/briefs

      - name: Collect Korea tech candidates
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
          NAVER_CLIENT_ID: ${{ secrets.NAVER_CLIENT_ID }}
          NAVER_CLIENT_SECRET: ${{ secrets.NAVER_CLIENT_SECRET }}
        run: |
          set -euo pipefail
          python3 scripts/collect-kr-feeds.py --mode daily-tech

      - name: Prepare KST runtime prompt
        id: prepare
        shell: bash
        run: |
          set -euo pipefail

          kst_now="$(TZ=Asia/Seoul date '+%Y-%m-%d %H:%M:%S %Z')"
          runtime_prompt="${RUNNER_TEMP}/kr-tech-daily-${GITHUB_RUN_ID}.md"
          report_file="reports/briefs/kr-tech-daily.md"

          KST_NOW="${kst_now}" RUNTIME_PROMPT="${runtime_prompt}" python3 - <<'PY'
          import os
          from pathlib import Path

          source = Path(".github/codex/prompts/kr-tech-daily-brief.md")
          destination = Path(os.environ["RUNTIME_PROMPT"])
          prompt = source.read_text(encoding="utf-8")
          prompt = prompt.replace("{{KST_NOW}}", os.environ["KST_NOW"])
          runtime = prompt + "\n\n## Runtime candidate files\n\n"
          runtime += "아래 후보 JSON 파일과 사용자 프로필을 읽고 최종 브리핑을 작성한다.\n\n"
          runtime += "- `reports/candidates/spring-study-topic.json`\n"
          runtime += "- `reports/candidates/ps-weekly-routine.json`\n"
          runtime += "- `reports/candidates/kr-oss-contribution-opportunities.json`\n"
          runtime += "- `reports/candidates/kr-dev-ai-news.json`\n"
          runtime += "- `reports/candidates/kr-ai-tech-news.json`\n"
          runtime += "- `reports/candidates/backend-practical-knowledge.json`\n"
          runtime += "- `configs/audience-profile.json`\n"
          runtime += "\n`ps-weekly-routine.json`을 알고리즘 루틴의 primary 후보로 사용한다.\n"
          runtime += "`spring-study-topic.json`은 1번 Spring Boot/JVM 학습 섹션에만 사용한다.\n"
          runtime += "`kr-dev-ai-news.json` 또는 `kr-ai-tech-news.json`에서만 4번 한국 최신 개발/AI 뉴스를 고른다.\n"
          runtime += "4번 뉴스에는 Spring 공식 문서, GitHub issue, Programmers 링크를 사용하지 않는다.\n"
          runtime += "`backend-practical-knowledge.json`의 today 후보를 5번 주니어 백엔드 실무지식에 사용한다.\n"
          runtime += "`## 오늘 " "할 일` 섹션은 출력하지 않는다.\n"
          runtime += "\n## Runtime output\n\n"
          runtime += "- 최종 Markdown은 `reports/briefs/kr-tech-daily.md`에만 작성한다.\n"
          runtime += "- 후보 JSON, progress, config, workflow, script 파일은 runtime Codex 생성 단계에서 수정하지 않는다.\n"
          runtime += "- Secret 값이나 Webhook URL은 출력하지 않는다.\n"
          destination.write_text(runtime, encoding="utf-8")
          PY

          {
            echo "kst_now=${kst_now}"
            echo "runtime_prompt=${runtime_prompt}"
            echo "report_file=${report_file}"
          } >> "${GITHUB_OUTPUT}"

      - name: Generate Korea tech daily Markdown brief
        uses: openai/codex-action@v1
        with:
          openai-api-key: ${{ secrets.OPENAI_API_KEY }}
          prompt-file: ${{ steps.prepare.outputs.runtime_prompt }}
          output-file: reports/briefs/kr-tech-daily-codex-summary.md
          sandbox: workspace-write
          safety-strategy: drop-sudo
          codex-args: '["--model","gpt-5.4-mini","-c","model_reasoning_effort=\"low\""]'

      - name: Validate Korea tech daily Markdown
        shell: bash
        env:
          REPORT_FILE: ${{ steps.prepare.outputs.report_file }}
        run: |
          set -euo pipefail
          python3 scripts/validate-career-feed-brief.py "${REPORT_FILE}" --type daily-tech

      - name: Send Korea tech daily brief to Discord
        shell: bash
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_KR_TECH_DAILY }}
          REPORT_FILE: ${{ steps.prepare.outputs.report_file }}
        run: |
          set -euo pipefail
          python3 scripts/send-discord.py "${REPORT_FILE}"

      - name: Commit Programmers assignment progress
        shell: bash
        run: |
          set -euo pipefail

          if git diff --quiet -- data/ps-progress.json; then
            echo "No PS progress changes to commit."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/ps-progress.json
          git commit -m "chore(ps): record daily Programmers assignment"
          git push

      - name: Upload Korea tech daily artifacts
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: career-feed-kr-tech-daily-${{ github.run_id }}
          path: |
            reports/candidates/spring-study-topic.json
            reports/candidates/ps-weekly-routine.json
            reports/candidates/kr-oss-contribution-opportunities.json
            reports/candidates/kr-dev-ai-news.json
            reports/candidates/kr-ai-tech-news.json
            reports/candidates/backend-practical-knowledge.json
            reports/briefs/kr-tech-daily.md
            reports/briefs/kr-tech-daily-codex-summary.md
          if-no-files-found: warn
